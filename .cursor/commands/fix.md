# /fix

用于快速修复当前问题（lint、类型、测试失败或明显 bug）。

## 执行步骤

1. 先复现问题并定位最小影响范围。
2. 实施最小改动修复，不顺带做无关重构。
3. 对改动文件执行检查（至少 `vp lint`，必要时 `vp test` / `vp check`）。
4. 输出修复说明：根因、改动点、验证结果、潜在风险。

## 约束

- 遵守仓库规则：使用 `vp`，不要直接用 `pnpm` 执行等价命令。
- 代码/测试/注释使用英文；文档可中文。
