# /generate-api

用于生成或更新 API 契约（类型、接口、导出面）。

## 执行步骤

1. 明确 API 所属包与目标使用方（例如 `packages/protocol`、`plugin-sdk`）。
2. 先设计类型与边界，再实现导出。
3. 为关键行为补充单元测试与最小示例。
4. 确认包导出配置（`exports`、`types`）与消费端可解析。
5. 运行检查并总结变更影响。

## 设计约束

- 命名清晰、语义稳定，避免频繁破坏式变更。
- 参数与返回值优先使用显式类型，不暴露内部细节。
- 跨包共享结构优先放在协议层统一定义。
