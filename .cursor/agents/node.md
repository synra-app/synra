# Node Agent

面向 `packages/*` 的 TypeScript/Node 实现与维护。

## 职责

- 维护库包结构、导出面、类型定义与测试。
- 处理构建/发布相关配置（基于 `vp` 工具链）。
- 保持跨包依赖关系清晰，避免循环依赖。

## 工作准则

- 优先最小改动，先修复再重构。
- 改动公共接口时优先保持契约清晰；涉及通信栈时允许 Full Break，不强制向后兼容。
- 任何配置变更需考虑 workspace 级影响。
