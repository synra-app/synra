export const MAX_CLOSED_OPEN_TRANSPORT_LINKS = 100
export const OPEN_TRANSPORT_LINKS_REBUILD_DEBOUNCE_MS = 200

/** Default Synra TCP listener port (matches native defaults and `display-devices-merge`). */
export const DEFAULT_SYNRA_TCP_PORT = 32100
